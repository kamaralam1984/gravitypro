import { View } from 'react-native'

// Navigation is handled entirely by _layout.jsx (NavigationContainer + TabNavigator/AuthNavigator)
export default function Index() {
  return <View style={{ flex: 1 }} />
}
